git init
git status
git add "nome"
git add .
git commit -m "commit inicial"
git config --global user.email "cbm@cbm.com.br"
git config --global user.name "CBM"
git commit -m "commit inicial"

git remote add origin https://github.com/Cleyson/projeto-cleyson-dev
git push
git push --set-upstream origin master

git reflog
git reset --hard 1658aa4

git branch
git checkout staging

git pull
git merge staging

git push --force

1.git pull da branch principal
2.gerar uma nova branch a partir da branch principal
3.Trabalhar  e adicionar novas funcionalidades na nova branch que criou
4.Finalizar o trabalho na branch temporária
5.Git checkout na branch principal
6.Git pull
7.Mergiar(unir) o código da branch temporária com principal(depois
de testar)
8.Git pish da branch principal

